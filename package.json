{
  "name": "milleteknik-demo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "prisma:generate": "pnpm --dir prisma prisma:generate",
    "prisma:migrate": "pnpm --dir prisma prisma:migrate",
    "prisma:seed": "pnpm --dir prisma prisma:seed"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
